import mongoose from "mongoose";

const documentSchema = new mongoose.Schema({
    name: {
        type: String,
        required: true,
    },
    type: {
        type: Sele,
        required: true,
    },
    image: {
        type: String,
        required: true,
    },
});

